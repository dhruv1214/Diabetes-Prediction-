import java.lang.Integer as Integer1

fun main(args: Array<String>)
{
    println(message = """Enter your name""")
    val name = readLine()
    println("enter your age")
    var age : Int
    age = Integer.Valueof(readLine())
    println("Your name is $name and your age is $age")
}